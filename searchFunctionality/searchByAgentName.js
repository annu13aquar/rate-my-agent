describe('Search by a agent name', () => {
  "use strict";

  it('should return matching location names, agent and agent names', () =>{

    browser.get(browser.params.URL);

});

afterAll(function(){
    browser.quit();
  });
});
